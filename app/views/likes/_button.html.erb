<% if current_user.like?(comment) %>
  <%= button_to "Dislike", dislike_path(comment_id: comment.id, user_id: current_user.id), remote: true, method: :delete  %>
<% else %>
  <%= button_to "Like", like_path(comment_id: comment.id, user_id: current_user.id), remote: true  %>
<% end %>